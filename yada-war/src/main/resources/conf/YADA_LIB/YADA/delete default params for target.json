{
  "query": "delete from YADA_PARAM where target = ?v",
  "params": [
    {
      "name": "pl",
      "value": "Gatekeeper",
      "spec": {
        "predicate": "user=getLoggedUser()",
        "policy": "E",
        "columns": [
          "userid:getLoggedUser()"
        ],
        "protector": "YADA crud protector",
        "type": "allow"
      },
      "rule": 1
    }
  ]
}